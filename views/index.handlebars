<h1>Burgers!</h1>

<h2>Burgers that are not eaten!</h2>

<ul>
  {{#each burger}}
    {{#unless eaten}}
      {{> burger/burger-block ate=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burger that are eaten!</h2>

<ul>
  {{#each burger}}
    {{#if eaten}}
      {{> burger/burger-block ate=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eaten">eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> Eaten!<br>
    <input type="radio" name="eaten" value="0"> Eaten
  </div>

  <button type="submit">Add Burger</button>
</form>
